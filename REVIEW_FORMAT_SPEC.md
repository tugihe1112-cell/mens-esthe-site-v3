# 口コミ投稿UI仕様 — メンエスマップ（mens-esthe-map.jp）

> **投稿画面がユーザーに何を見せ、何を問いかけ、どう書かせるか**の仕様書。
> 画面の文言はすべて実装から抽出した実物です。
> 最終更新: 2026-09-04

---

## 全体像

投稿は **4ステップ**。画面上部に常にプログレスバーと「あと◯ステップで完了」が出ます。

```
[1] 誰の口コミか   →  [2] 採点する  →  [3] 体験談を書く  →  [4] 確認して投稿
     店舗・セラピスト     6軸スライダー     4つの欄            公開される形を見せる
                          ＋一言コメント    ＋文字数メーター
```

- **入力は自動保存**（1秒のdebounce・7日間保持）。途中で閉じても続きから書けます
- 画面下部に固定の保存インジケータ（「保存中…」→「✓ 下書き保存済み 18:42」）
- 投稿フロー中はサイトのフッターとボトムナビを非表示（CTAとの衝突を避けるため）

---

## Step 1 — 誰の口コミか

**見出し**: 店舗・セラピストを選択

1. **店舗**を検索して選ぶ
   - プレースホルダー: `店舗名を入力して検索...`
   - 入力するとインクリメンタルに候補が出る（未入力時は先頭20件）
2. **セラピスト**をカードから選ぶ
   - 絞り込み欄: `セラピスト名で絞り込み...`
   - グリッド末尾に「✏️ リストにいない」ボタン
3. 「リストにいない」を選ぶと入力欄に切り替わる
   - プレースホルダー: `セラピスト名を入力してください`
   - この場合カード一覧は隠れ、「← リストから選ぶ」で戻れる

---

## Step 2 — 採点する

**見出し**: 採点 / `スライダーを動かして採点してください`

画面上部に**総合スコアが大きな円**で表示され、スライダーを動かすとリアルタイムに変わります
（4.5以上=ピンク / 3.0以上=白 / それ未満=グレー）。

### 6軸のスライダー（すべて必須・1〜5）

| 順序 | アイコン | 軸 |
|:---:|:---:|---|
| 1 | 💎 | ルックス |
| 2 | 👙 | スタイル |
| 3 | 💆‍♀️ | 技術 |
| 4 | 🥰 | 接客 |
| 5 | 🔥 | 密着度 |
| 6 | ✨ | 清潔感 |

### 各軸の直下に「一言コメント」欄（任意・120字まで）

プレースホルダー: `{軸名}について一言（任意）` → 例:「ルックスについて一言（任意）」

**これが設計の要点です。** スライダーを動かした直後は、その軸について
考えている文脈がまだ頭にあるので、1行書く負担がほぼありません。
点数の**理由**なので体験談と重複しません。

欄の下に緑のボックスで:

> 一言はすべて任意です。書いた分は文字数に加算され、口コミの一部として公開されます。
>
> （何か書くと）✓ 一言コメント **35文字** は文字数に加算され、口コミの最後に一緒に公開されます。

### タグ（任意）

見出し: `タグ（任意）`。カテゴリ別のボタンから選択（複数可）。

| カテゴリ | 選択肢 |
|---|---|
| ■体型 <br><small>まずは好みのスタイルから探す</small> | スレンダー / グラマー / 巨乳 / 美脚 / 小柄 / 高身長 |
| ■雰囲気 <br><small>一緒に過ごす時間をイメージ</small> | 可愛い系 / 美人系 / 清楚系 / ギャル系 / お姉さん系 |
| ■年代 <br><small>年齢層で絞り込む</small> | 10代 / 20代前半 / 20代後半 / 30代 / 40代 |
| ■属性 <br><small>その他のこだわり</small> | 色白 / 健康的 / ベテラン / 外国人 |

**この20個以外は選べません。** 検索の絞り込みと連動しているためです。

---

## Step 3 — 体験談を書く（本体）

**見出し**: 体験談を書く / `あなたの体験を日記のように記録しましょう`

冒頭に緑のボックス:

> 💾 **一度に書き切らなくて大丈夫です。** 入力は自動保存されるので、途中で閉じても続きから書けます

### 文字数メーター（2段ゴール）

```
合計文字数                                    342 文字
✓ 採点の一言コメント 35文字を含みます
━━━━━━━━━━━━━┃━━━━━━━━━━━━━━━━━━━━━━━
[ ✅ 200字・3日分確定！ ]  [ 700字で7日（あと358） ]
```

- 200字を超えると琥珀色、700字を超えると緑になる
- 未達のときは「あと◯文字」と残りが出る
- **採点の一言コメントも加算される**ことを明示（書き損にならないと伝えるため）

### 4つの入力欄（縦のタイムライン上に配置）

左端にグラデーションの縦線が走り、各欄が時系列上の点として並びます。

---

**1. `ENTRANCE` — 入店・受付** ※必須

> プレースホルダー: お店の雰囲気や、受付の対応はいかがでしたか？（10文字以上）
>
> 💡 例: `立地・行きやすさ` `内装・清潔感` `香り・音・第一印象`

---

**2. `MEETING` — ご対面** ※任意

> プレースホルダー: パネル写真との違いや、第一印象を教えてください。
>
> 💡 例: `写真との差` `見た目の印象` `会話・気遣い`

---

**3. `SESSION` — 施術・接客** ※任意

> プレースホルダー: マッサージの技術や、会話の盛り上がりはどうでしたか？
>
> 💡 例: `技術の丁寧さ` `強弱・時間配分` `要望への対応`

---

**4. `CONCLUSION` — 総評** ※必須

> プレースホルダー: 満足度や、またリピートしたいか教えてください。（10文字以上）
>
> 💡 例: `満足度` `どんな人向き` `また行きたいか`

---

⚠️ **採点コメント用の入力欄はここに出ません。** Step2 の一言から自動で組み立てられます
（出すと同じことを2回書かせることになるため）。

---

## Step 4 — 確認して投稿

**見出し**: 投稿内容の確認 / `この内容で投稿しますか？`

- 対象セラピスト名をピルで表示
- `内容を確認して、問題なければ投稿してください。`
- `※投稿後の修正はできません。`

**採点コメントを書いていた場合、実際に公開される形をここで見せます**（不意打ちを避けるため）:

> **採点コメント**
> 採点欄に書いた一言です。**体験談の最後に、この形で一緒に公開されます。**
> ・ルックス（★2）写真とは別人だった
> ・技術（★4）強さの確認が丁寧だった

送信ボタン: `🚀 投稿を完了する` （送信中は `投稿中...`）

### 投稿完了後

リダイレクトせず完了画面を表示:
- 付与された閲覧日数（700字なら7日、200字以上なら3日／次の7日まであと◯文字）
- 「投稿した口コミを見る」リンク
- 「読まれたら週次メールで通知」の案内

---

## 出来上がる口コミの形

4つの欄と採点コメントが、**この順序で連結**されて1本の本文になります。

```
（入店・受付に書いた文）

（ご対面に書いた文）

（施術・接客に書いた文）

（総評に書いた文）

ルックス（★2）写真とは別人だった
技術（★4）強さの確認が丁寧だった
```

公開ページでは各区分が見出し付きで表示され、採点コメントは区切り線の下に箇条書きで出ます。

---

## この設計の意図

**なぜ時系列の4区分なのか**

入店 → 対面 → 施術 → 総評 は、体験した順序そのものです。記憶を辿る順序と一致するので
書き手の負担が小さい。属性別（ルックス・技術…）に分けると記憶を分解し直す作業が発生して手が止まります。
属性ごとの評価は Step2 の6軸が担当しているので、役割が重複しません。

**なぜ必須が2つだけなのか**

入口（entrance）と出口（exit）だけを必須にしています。中間の2つを任意にすることで、
「全部埋めないと出せない」という圧を消しつつ、**読み手にとって最も価値がある
"最初の印象"と"また行くか"は必ず入る**ようにしています。

**なぜ200字と700字の2段なのか**

最低ラインは元々700字でしたが、着手の崖が高すぎたため200字に下げました。
ただし700字を撤廃せず「閲覧権7日」の報酬ラインとして残し、長文を誘導しています。

**なぜ書き出しヒントがチップ形式なのか**

空欄を前にして手が止まる（空白恐怖）のを防ぐためです。文章例を出すと
それをなぞられて似た口コミが量産されるので、**観点だけ**を短い語で提示しています。

---

## 書き手が実際に問われていること（要約）

執筆時にはこの7点に答える形になります。

1. **どの店の、誰の話か**
2. **6つの軸で何点か**（ルックス・スタイル・技術・接客・密着度・清潔感）
3. **その点数の理由は何か**（各軸1行・任意）
4. **店に入ったとき、どうだったか**（立地・内装・受付の対応）
5. **会ったとき、どうだったか**（写真との差・第一印象・気遣い）
6. **施術中、どうだったか**（技術・時間配分・要望への対応）
7. **結局どうだったか**（満足度・どんな人向きか・また行くか）

---

## 技術的な制約（補足）

UI の裏側で強制されている条件です。これを外れると投稿が拒否されます。

| 制約 | 内容 |
|---|---|
| 最低文字数 | 合計200字未満は投稿不可（フロントとDBの二重で拒否） |
| 文字数の数え方 | 4欄＋採点コメントを trim した長さの合計。区分間の空行は数えない |
| タグ | 上記20個の白リストのみ。範囲外は自動除去される |
| 6軸 | すべて1〜5の整数で必須 |
| 必須欄 | `entrance` と `exit`（各10文字以上） |
| 本文と区分の一致 | DBのCHECK制約で強制。区分から再構成した文字列が本文と一致しないとINSERTが失敗する |
| 公開可否 | クライアントからは指定不可。各セラピストの1件目のみDBトリガーが自動公開する |

定義元は `src/features/reviews/reviewStory.mjs`（区分と6軸）、
`src/data/constants.js`（タグ）、`src/pages/PostReviewPage.jsx`（画面）です。

---

## 内容の規約

技術制約ではありませんが、運営方針として `playbook/ng-rules.md` に定められています。

- 本物の一次体験のみ（架空・他サイトからの転載・AIによる書き直しは不可）
- 際どい部分は隠語・ぼかしで表現する。ただし密着や施術の質感自体は口コミの中核なので避けない
- 実店舗を名指しした違法行為の断定（本番の有無・ゴム常備・追加料金で◯◯可 等）はしない
- 金額の端数・固有の会話内容・駅名の細部など、特定につながる情報は避ける
- **辛口を書いてよい**。掲載店舗から広告費・掲載料を受け取っていないことが
  このサイトの差別化なので、★2の評価もそのまま掲載される
