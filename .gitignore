node_modules
dist
.DS_Store
.env
# ⚠️ .env のバックアップ・派生ファイルも必ず除外する。
#    2026-08-10、鍵ローテーション時に作った .env.backup_1786092315 を `git add -A` が拾い、
#    GitHubのプッシュ保護がAnthropic/Supabase/Resendの鍵を検出してpushを拒否した
#    （＝公開はされていないが、`.env` だけの指定では防げないと判明した）。
.env.*
!.env.example
.env.backup*
.vercel
migrate_to_supabase.js

# Next.js build output
.next/
out/

# GCPサービスアカウント鍵（絶対にcommitしない）
.gcp-metrics-key.json
*-key.json
scripts/metrics/cron.log

# 一時生成物（移行エラーログ・一時分析スクリプト等）
r2_copy_errors.json
outputs_gsc_analyze.py
output/

# 画像URLのバックアップ（audit_shop_images.mjs が --live 前に自動生成）。
# 195KB×実行回数ぶん増えるうえ、内容はDBの複製なのでリポジトリには入れない。
# ⚠️ ただし復旧に使うので、事故処理が終わるまでローカルから消さないこと。
shop_image_url_backup_*.json
