# スマホ・PCのUI/UX改修仕様

本書のU01〜U06はコンサルタントの提案仕様です。START.mdの作業単位ごとに実装します。現行の紺色背景・ピンクのブランド色、写真、coverflowを活かします。

## U01：文字・色・余白・共通ナビを統一する

対象：[index.css](/Users/okabayashi/Downloads/mens-esthe-site/src/index.css)、[Header.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/Header.jsx)、[BottomNav.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/BottomNav.jsx)、[Footer.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/Footer.jsx)。

### 指定値

`index.css`に専用の`.ui-*`クラスとCSS変数を追加し、今回触る画面から使用する。既存の`.card`や全`button`への一括上書き、全ファイルの`text-xs`機械置換はしない。

| 項目 | 指定 |
|---|---|
| 背景／カード／入力背景 | `#020617`／`#0f172a`／`#1e293b` |
| 主文／補助文／境界 | `#f8fafc`／`#cbd5e1`／`#334155` |
| 主ボタン | 背景`#be185d`、文字白、hover`#9d174d` |
| テキストリンク | `#f9a8d4`。常設の本文リンクは下線または明確なリンク形状 |
| 口コミ本文 | スマホ16px・PC16px、行高1.8、通常400。要約カードは14px・行高1.7 |
| 補助文 | 原則13px。日時・件数でも12px以上 |
| フォーム | ラベル14px/600、入力16px、説明13px、入力高48px以上 |
| 見出し | ページH1はスマホ28px/1.3、PC36px/1.3。セクション22px/1.4、カード名16px/1.5 |
| 角丸 | カード16px、入力・ボタン12px、チップ999px |
| 余白 | スマホ左右16px、PC左右24px。セクション間32px/PC48px、カード内16px/PC24px |
| 操作領域 | 主・副ボタン44px以上、フォーム主ボタン48px以上。アイコンだけの操作も44×44px |
| 最大幅 | 一覧・ホーム1200px、人物本文760px、登録448px |

操作領域44pxは本案件の設計値。WCAGの最低要件と同一だと説明しない。通常の文字のコントラストは4.5:1以上を検査する。[W3Cの説明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)

発光・大きな影・グラデーションを主ボタンとスライダーへ集約する。本文カード・フォーム背景は単色。全見出し・本文をfont-blackにせず、強調は見出しと主要操作だけ。読める文字を写真の明暗やぼかしに依存させない。

### ナビゲーション

1. ロゴ表記はヘッダーでも「メンエスマップ」に統一。既存のアイコンはそのまま使う。
2. PC（1024px以上）：左ロゴ、中央「セラピストを探す」「口コミを読む」、右「ログイン」「無料登録」。ランキングはフッターへ残す。登録済みは右を「マイページ」へ切替。
3. 1024px未満は共通ボトムナビを表示する。現在はヘッダーPCナビが`lg`、BottomNavが`md:hidden`のため768〜1023pxで主ナビが途切れる。BottomNavを`lg:hidden`へ揃える。中央ドックは最大幅600px、中央寄せ。
4. ボトムの5項目を「ホーム」「探す」「口コミ」「投稿」「登録／マイページ」にする。URLは順に`/`、`/search`、`/popular-reviews`、`/post-review`、未登録`/register`・登録済み`/mypage`。既存利用者のログインはヘッダーと登録画面に常設する。
5. 未登録で投稿だけを常時発光させない。選択中のみ強調し、未登録の「登録」は文字色ピンク。ラベル12px、高さ60px、下余白はsafe-area込み。`aria-current="page"`で現在地を表す。
6. 投稿フォーム・認証フォームでナビを隠す既存ルート判定は維持する。タブレットにBottomNavを出す変更と同時に、フッターの下余白の`md`境界も`lg`へ揃える。
7. ヘッダーは高さスマホ64px・PC72pxを基準に、上へ戻る時も本文・フォーカス中の要素を覆わない。F06の透過判定を先に直す。

各画面に固定CTAがある場合はBottomNavの上へ置く。最大2段まで。下余白を「ナビ60px＋外側8px＋safe-area＋CTA高＋CTAとの間8px」から計算し、本文末尾が隠れないようにする。

受入：360pxで5項目が読める。820pxでも探す・口コミ・投稿へ移動できる。未登録と登録済みで右端の意味が正しい。すべての主要操作に見えるfocus ringがある。

## U02：ホームを「価値が分かる入口」にする

対象：[Home.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/Home.jsx:214)、[SearchBar.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/SearchBar.jsx)、[HomeReviewCard.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/HomeReviewCard.jsx)、[TopHeroSlider.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/TopHeroSlider.jsx)。

### 上部の完成形

```text
共通ヘッダー
既存coverflow（位置・効果・写真・基本サイズを維持）
┌ 検索カード ───────────────────────┐
│ 口コミを読んで、店選びの不安を減らす。             │
│ 無料登録で3日間、口コミ読み放題                   │
│ [無料登録する]  登録せず口コミを読む              │
│ [店舗・エリア | セラピスト] ※スマホ              │
│ [検索入力................] [検索]               │
└────────────────────────────────┘
最新の口コミ 1件 → 中立宣言 → 地域別口コミ 最大2件
エリアから探す → 注目セラピスト → 新着店舗
```

1. 現在の検索カード見出し「店舗・セラピスト名で口コミ検索」を上記の価値説明へ変更する。H1をこの可視見出しへ移し、現在の`sr-only` H1は削除。ページ内H1は1つ。title/descriptionは変更不要。
2. このカード内ではH1をスマホ22px・PC28pxとし、汎用H1サイズより小さくする。カード幅はPC880px、余白スマホ16px・PC24px。既存のPC40px余白と40px角丸を縮める。
3. 未登録には上記特典と主CTAを表示。補助リンクは`/popular-reviews`。登録済みにはこの登録案内を出さず「店舗名・エリア・セラピスト名で探す」を表示する。
4. スマホの検索対象切替は残す。入力＋検索ボタンを同じ行にして高さ48px、ボタン幅72px、間隔8px。入力`min-width:0`。PCは既存の店舗＋人物の2入力を同じ行に残し、その右に検索ボタン幅96px。
5. 検索欄は常時ラベルを持ち、Enterでも検索できる。モード変更で検索値を消す現行仕様をこの見た目変更で別仕様にしない。
6. 登録特典の詳細として「閲覧期間は登録手続き時から3日間です。メール確認後に利用できます」を13pxで表示。メール確認完了から丸3日とは書かない。無料期間後の自動課金は実装されていないので「自動課金なし」を補助文として使用可。

**スライダーの置換・静的化・coverflow効果や高さの再設計はこの作業に含めない。** ファーストビューへ全要素を詰めるために文字を小さくしない。390×844では特典と登録CTAと検索操作まで見えることを優先し、最新口コミ本文が初期画面に入ると保証しない。

### 下部の整理

1. 最新1件→中立宣言→地域別口コミの順序を維持。中立宣言は「掲載店舗から広告費・掲載料を受け取っていません。」を残す。掲載母数は既存の実取得値を使用し、口コミ件数のように見せない。
2. 地域別は全県分を縦に並べず、見出し＋`select`「口コミの地域」＋選択地域の最大2件にする。選択肢は既存`orderedPrefs`から生成。初期値はその先頭。最新カードとIDが重複する口コミは除く。
3. 2件に満たない場合はある分だけ表示。「この地域の口コミを見る」から既存エリアURLへ送る。別の地域の口コミで埋めない。全エリアの内部リンクは既存「すべてのエリア」とフッターに残す。
4. 現在の4機能ショートカットと、その後の「新人キャスト／みんなの口コミ」の重複バナーは外す。主要導線は共通ナビ・検索・地域ブロックで確保する。リンク先のページ自体は削除しない。
5. 投稿バナーはホーム下部の1か所へ集約する。未登録時は「無料登録で3日間、口コミ読み放題」＋登録CTA、登録済みは「体験談を投稿して、閲覧期間を延長」＋投稿CTA。「1件で読み放題」だけの期限なし表現は使わない。
6. PC右サイドバーにある投稿・口コミ・統計の大きなカード群は、登録案内1枚＋統計テキストリンクへ整理。別の固定追従バナーは追加しない。
7. 新着店舗・人物写真の横スライドは残す。自社で測っていない「人気」「No.1」は付けない。

### ホーム口コミカード

スマホでは写真80×104px＋店舗・人物・評価を上段に配置し、要約本文をカード全幅の下段へ出す。写真右側の細い列へ本文を閉じ込めない。店舗名は16pxで2行、人物名14px、日時と投稿者12px。補助情報は値がある場合だけ表示。

現行「続きを読む」→300字取得→「全文を読む」の二段階を、通常のリンク「口コミ全文を読む」1回へ変更する。リンク先は既存人物URL＋`#review-{口コミID}`。取得前にボタンを押したまま待つ中間状態をなくす。`HomeReviewCard`の`body/loading/expanded/handleExpand`と、そのためだけのSupabase取得を削除できる。公開範囲は変えない。

評価6軸はカード内で3列×2行、値のない軸を0点にしない。詳しい採点コメントは人物詳細に残す。口コミが0件なら架空のカードを作らず、「公開口コミを探す」「店舗を探す」の導線を表示。

受入：上部で「何のサイトか・無料特典・検索方法」が読める。本文まで1クリックで進める。モバイルの地域別口コミは最大2件。PC右カラムが空白の長い柱にならない。既存の口コミ・エリアURLへ辿れる。

## U03：登録・ログインフォームを読みやすくする

対象：[RegisterPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/RegisterPage.jsx:55)、[LoginPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/LoginPage.jsx)。F01/F02と連携する。

1. 現在の中央寄せ・最大448pxを維持。PCだけ大きな広告用2カラムへ作り替えない。背景は紺＋薄いグラデーション1枚、常時動く800pxの発光レイヤーは削除する。
2. ロゴは24px、ページ見出しH1は「無料会員登録」。ロゴをH1にしない。H1の下に「登録で3日間、口コミ読み放題」、補助文「メール確認後に利用できます。閲覧期間は登録手続き時から3日間です。」。
3. 入力欄はスマホ・PCとも全て1列。順番は表示名→メール→パスワード→確認。確認欄を残し、入力高48px、列間16px、カード内スマホ20px・PC24px。
4. 各入力に固定`id/name`、labelの`htmlFor`、`required`。表示名は既存の1〜30字・ニックネームの説明を維持する。
5. メールは`type=email`、`autoComplete=email`、`autoCapitalize=none`、`spellCheck=false`。登録の両パスワードは`autoComplete=new-password`、ログインは`current-password`。
6. パスワードそれぞれに「表示／隠す」ボタン。`type=button`、44px、aria-labelで対象欄を区別。切替で値を消さない。パスワードマネージャーと貼り付けを妨げない。
7. 主ボタンは「確認メールを送る」、送信中「送信中…」。入力→メール確認の2段階があることをボタン直前に表示する。API成功は登録完了と表示せず、送信完了画面へ切り替える。
8. 利用規約・プライバシーポリシーは新しいタブで開き、入力を保持。既存の同意チェックを省略・初期ONにしない。
9. エラーは入力直下に13px、`aria-invalid/aria-describedby`を設定。送信後に最初の不正欄へfocus。フォーム全体の通信エラーは`role=alert`。既存入力を消さない。
10. 重複メールは「このメールアドレスは登録済みです。ログインしてください」＋戻り先付きログイン。429は「時間をおいて、もう一度お試しください」。503/通信失敗は「送信できませんでした。入力内容はそのままで、もう一度お試しください」。内部例外文を利用者にそのまま出さない。

### メール送信後

見出し「確認メールを送信しました」、宛先メール、手順「1. メールを開く 2. メール内のリンクを押す」、補助文「届かない場合は迷惑メールフォルダも確認してください」。戻り先がある場合は「確認後、先ほどのページに戻ります」と表示。

既存アカウントが生成済みなので、単純な「メールを修正して再登録」は新規アカウントを増やす。今回その機能は追加せず「メールアドレスを間違えた場合はお問い合わせ」リンクと、戻り先付きログインを置く。再送ボタンはバックエンドがないまま作らない。

受入：labelクリックで入力にfocus。360pxでもパスワードを確認できる。Tab順が自然。エラー理由が欄と結び付く。送信受付と確認完了が混同されない。

## U04：口コミを読んだ人向けの登録導線

対象：[ReviewListWithRestriction.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/ReviewListWithRestriction.jsx:33)、[ModernReviewCard.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/components/ModernReviewCard.jsx:180)、[ThreadDetailPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/ThreadDetailPage.jsx)。

共通の登録案内を小さなコンポーネントに切り出してもよい。必要propsは`source, returnTo, shopId, therapistId`。`returnTo`の契約はF01を使用し、別方式を作らない。

| 状態 | 主表示・主操作 | 副操作 |
|---|---|---|
| 未登録・公開口コミを読了 | 「気になる口コミを、もっと読む」／「無料登録で3日間、口コミ読み放題」／「無料登録する」 | 「ログイン」、小さいリンク「体験談を投稿する」 |
| 登録済み・閲覧権あり | 「あなたの体験談も共有しませんか」／「体験談を書く」 | 特典は200字3日・700字7日を正確に表示 |
| 登録済み・閲覧権切れ | 「体験談を投稿して、閲覧期間を延長」／「体験談を書く」 | 200字で3日間、700字で7日間 |
| ロックカードが実際に描画される場合 | 未登録なら登録CTA、会員なら投稿CTA | 閲覧可否判定は変更しない |

公開口コミが読めている地点では「登録してこの続きを読む」と書かない。登録しないと現在の本文を読めないという誤解になる。公開本文へ新しいゲートを追加しない。会員向け口コミ件数を取得していない場合は、件数や「全○件」を捏造しない。

読了案内はリスト末尾に1枚。カードの途中へ登録ポップアップを割り込ませない。スマホ主ボタン全幅48px、PCは幅240px以上。ログインリンクにも44pxの操作領域。

人物詳細の固定投稿CTAは未登録で「無料登録する」へ変更する。画面内に読了案内の主ボタンが見えている間は固定CTAを隠す。IntersectionObserverで検出し、スクロールの毎フレームstate更新はしない。登録済みの固定投稿は現在の対象指定を維持。

`ModernReviewCard`のarticleに`id="review-{id}"`、`scroll-margin-top:96px`を付け、F01の登録復帰・ホームからのリンクで同じ口コミを開けるようにする。指定された公開口コミは折り畳みを開く。権限のない本文をURLだけで開かない。

受入：未登録で読了→登録→メール確認後に同じ口コミへ戻る。登録後は登録CTAが残らない。既存会員は投稿できる。BottomNavと固定CTAで本文を覆わない。

## U05：検索・店舗・人物ページの比較しやすさ

対象：[SearchPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/SearchPage.jsx)、[ShopListPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/ShopListPage.jsx)、[ShopDetailPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/ShopDetailPage.jsx)、[ThreadDetailPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/ThreadDetailPage.jsx)、[PopularReviewsPage.jsx](/Users/okabayashi/Downloads/mens-esthe-site/src/pages/PopularReviewsPage.jsx)。

### 検索

- PC上部の店舗・人物の2入力を維持し、結果上部の3本目「キャスト名で絞り込み」を削除する。`castNameFilter`とそれだけを使うAND条件を除去し、人物名は`castInput`へ一本化。
- 上部に可視のH1「セラピストを探す」。入力のラベルは「店舗名・エリア」「セラピスト名」。検索中でも現在の入力を表示する。
- 320〜639pxは2列、640〜1023pxは3列、1024〜1279pxは4列、1280px以上は5列。間隔スマホ12px・PC20px。既存24件ずつの追加表示を維持する。
- 写真は3:4。名前・店舗・地域は写真下の単色面へ移す。名前16px、店舗/地域13px、2行まで。写真の上にはお気に入り等の操作だけを置く。
- 口コミ件数はF04で人物一致が保証された公開データだけを表示。未取得を「0件」と表示しない。ランキングと件数順も同じ集計を使用する。
- 「店舗・地域が偏らないように表示しています」は実装説明なので削除し、「気になるセラピストから探せます」を短い1行の案内として残す。
- 条件は結果上部に解除可能なチップで表示。初期未入力の注目一覧は残す。タグを選んだ状態で人物名を変更しても、条件が隠れたまま残らない。
- スマホのタグシートはrole=dialog、aria-modal、見出し関連付け、初期focus・Tab循環・Escape・閉じた後のfocus復帰を実装。既存の背景スクロール停止を維持する。

### 店舗一覧

- `ALL SHOPS`→「店舗を探す」、`SEARCH RESULTS`→「店舗の検索結果」。件数は「表示対象○件」とし、総掲載数との差を勝手にデータ欠落と判断しない。
- モバイルの横長カードを維持。画像幅104px・カード最小高140px、右の店舗名16px・地域/住所13px。カード全体は通常リンク。
- ロゴ・バナーは`LazyImage`の`imgClassName`にcontainを指定。人物写真のcoverと区別する。画像がない場合は店名頭文字、空箱にしない。
- 全カードの固定「★ New」を削除。系列店の支店名・地域を省略しすぎない。元の店舗レコードやURLを見た目だけの理由で統合・削除しない。

### 店舗詳細

- 左タグ＋セラピスト一覧、全セクション縦積み、上部のアンカーリンクを維持。セクションを表示切替するタブへ作り替えない。口コミ0件でも同じレイアウト。
- 店舗詳細のヒーローはホームcoverflowと別物。ここはスマホ高280px・PC340pxを基準に圧縮し、長い店舗名は28px/36pxで2行、必要なら高さを自然増加させる。巨大な60px級見出しで末尾1文字だけ折り返さない。
- 店舗画像のcontainとぼかし背景は維持。公式店舗画像かどうか疑わしいものはF09へ回し、別の店の写真で代替しない。
- PCコンテンツ幅を現状`max-w-4xl`から1200pxへ、左タグ220px・間隔24px・残りを一覧にする。人物カードはスマホ2列、タブレット3列、PC4列。カード情報は検索と同じ並び。
- `THERAPISTS`→「在籍セラピスト」、`SHOP INFORMATION`→「店舗情報」、`PRICE`→「料金」、`ACCESS`→「所在地」、`GROUP STORE`は利用者に必要な系列情報を確認できる場合だけ「系列店」へ、`Save`→「お気に入り」。
- 口コミ0件のタグは0件のまま表示し、選択不可にする。「口コミが増えると絞り込めます」を補助文にする。タグ領域自体は隠さない。
- 「口コミを読む」アンカーを投稿ボタンと同程度に見つけやすくする。投稿CTAは1つを基本とし、同じ位置の重複を外す。公式出勤の表示はF07による。

### 人物詳細・口コミ一覧

- 人物名・店舗名・口コミ件数を写真の隣に表示。スマホ写真幅112px・最大高160px、PC写真幅200px。大きな人物写真を追加して本文を押し下げない。
- 6軸評価は3列×2行。口コミ本文16px/行高1.8、区分見出し16px/600。透かし装飾は読み上げ対象外`aria-hidden=true`とし、本文のコントラストを下げない。
- 口コミ一覧は要約14px/1.7、人物名16px、店舗/日時13px、全文リンク44px以上。全文リンクは対象口コミのアンカー付き。訪問日と投稿日を混同しない。
- 口コミを読むのに不要な写真上のグラデーション・引用符の巨大装飾・重複ロゴを減らす。実口コミの本文、採点、タグは書き換えない。

受入：360pxで店舗名・人物名・口コミ有無が判別できる。1440pxで本文が横に長すぎず、店舗一覧の余白が不自然に広くない。未入力、検索一致、0件、エラーを確認する。

## U06：登録の効果を測る

対象：[analytics.js](/Users/okabayashi/Downloads/mens-esthe-site/src/utils/analytics.js)、登録・確認画面、U02/U04のCTA。既存のGA4送信関数を使い、計測SDKを追加しない。

| イベント | 発火条件 |
|---|---|
| `register_cta_click` | 登録CTAクリック。sourceはhome/header/bottom_nav/review_end/review_lock/favoriteの許可値だけ |
| `register_view` | 登録画面の表示。再レンダーでは重複させない |
| `register_start` | 最初の入力変更時、フォーム表示1回につき1回 |
| `register_submit` | クライアント検証成功後、登録APIを呼ぶ直前 |
| `register_email_sent` | APIがメール送信成功を返した時 |
| `register_error` | validation/duplicate/rate_limit/network/serverという固定分類 |
| `registration_confirmed` | signup確認経路で有効な確認済みセッションを確認した時 |
| `registration_return` | 確認後に戻り先へ到着。page_typeのみ記録 |

F01の新しい確認後ページとF02のOTP確認ページの両方から同じ完了イベントを重複発火しない。完了イベントの担当は確認後ページ1か所にする。同一ブラウザの再表示は記録済みキーで抑える。ただしこれは完全なユーザー重複排除ではないので、GAイベント数を確定した新規会員数と同一視しない。

メール・氏名・パスワード・トークン・本文・検索語・生のURLをイベントへ送らない。`source`は固定の許可値だけ。確認ページの自動page_viewにもtokenやnext入りの生URLが載らないことを確認し、必要なら認証経路では自動送信を止めて安全なパスだけで送る。

見る数字は、登録画面→入力開始、送信→メール送信成功、確認完了、確認後復帰。端末区分はmobile/desktopで分ける。集計窓は改修前後各14日、母数と件数を併記し、少数なら傾向として扱う。効果保証や根拠のない目標CVRは置かない。
